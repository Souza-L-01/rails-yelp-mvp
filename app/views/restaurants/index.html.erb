<h1>Restaurants</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <br>
    </tr>
  </thead>
  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= link_to restaurant.name, restaurant_path(restaurant), class: "btn btn-primary"  %></td>
        <td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

